extends layout 

block content 
    h1 #{title}

    if user
        if user.creatorPrivilege

            form(method='POST' action='') 
                div.form-group  
                    label(for='name') Category: 
                    input#name.form-control(type='text', placeholder="Liens, Judgments, etc." name='name' required='true' value=(underfined===category ? '' : category.name) ) 
                button.btn.btn-primary(type='submit') Submit 
        
            if errors 
                ul 
                    for error in errors 
                        li!= error.msg 
    
        else 
            p You do not have privileges to create new categories.
    else 
        p Log in to access this page.